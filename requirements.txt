# RunPod SDK
runpod>=1.7.0

# FastAPI (kept for local dev via main.py)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# PDF processing
PyMuPDF>=1.24.0
Pillow>=10.3.0

# AI / ML
transformers>=4.46.0
torch>=2.3.0
accelerate>=0.30.0

# Environment
python-dotenv>=1.0.0

# NOTE: llama-cpp-python is installed separately in Dockerfile
# with CUDA flags: CMAKE_ARGS="-DGGML_CUDA=on"